import { logger } from "../../constants/constants"
import { userModel } from "./user.model"
import { connexion_request } from "./user.request"

export const authentification = () => { }

export const inscription_service = () => { }

export const connexion_service = (toStore: userModel) => {


    logger('connexion', "connexion action")
}

export const update = () => { }

